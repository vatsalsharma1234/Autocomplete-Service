# Autocomplete-Service
A lightweight Autocomplete API that returns top-k phrase suggestions for a given prefix using a Trie with per-node frequency ranking; easy to run locally, extendable to larger datasets, and interview-friendly to explain algorithms and tradeoffs.
